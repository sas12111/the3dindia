services:
  - type: web
    name: the3dindia-backend
    env: node
    plan: free
    root: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
