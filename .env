# Frontend environment variables
# In production leave these blank so the app uses relative `/api` paths.
# Set these in Vercel only if you need absolute URLs (e.g. https://the3dindia.vercel.app)
REACT_APP_API_URL=
VITE_API_URL=
